spring:
  application:
    name: habit-tracker

  datasource:
    url: jdbc:mariadb://localhost:3306/habit_tracker
    username: habit_user
    password: 1234

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true